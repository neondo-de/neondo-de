"use client";
import { useEffect,useState } from "react";import {getSupabase} from "@/lib/supabase";import {ArrowLeft,CalendarDays,Clock3,MapPin} from "lucide-react";
export default function Schedule(){const supabase=getSupabase();const [items,setItems]=useState<any[]>([]);const [loading,setLoading]=useState(true);useEffect(()=>{(async()=>{const {data:{user}}=await supabase.auth.getUser();if(!user){location.href="/";return}const {data}=await supabase.from("applications").select("id,status,shift_id,shifts(id,title,role,start_at,end_at,location,hourly_rate)").eq("worker_id",user.id).order("created_at",{ascending:false});setItems(data||[]);setLoading(false)})()},[]);if(loading)return <div className="dashboard-loading">Loading your schedule…</div>;return <main className="schedule-page"><header className="detail-nav"><a href="/dashboard"><ArrowLeft size={18}/> Back to discovery</a><div className="brand"><span className="brand-mark">N</span><span>NEONDO</span></div></header><section className="schedule-content"><span className="eyebrow">YOUR SCHEDULE</span><h1>My <em>shifts.</em></h1><p>Keep track of the opportunities you've applied for.</p>{items.length?items.map(a=>{const s=a.shifts;return <article className="schedule-item" key={a.id}><div className="schedule-date">{s&&new Date(s.start_at).toLocaleDateString("en-DE",{weekday:"short",day:"numeric",month:"short"})}</div><div><h3>{s?.title||"Shift"}</h3><p>{s?.role} · <Clock3 size={12}/>{s&&new Date(s.start_at).toLocaleTimeString([], {hour:"2-digit",minute:"2-digit"})} · <MapPin size={12}/>{s?.location||"Berlin"}</p></div><span className={`status ${a.status}`}>{a.status}</span></article>}) : <div className="empty"><CalendarDays size={25}/><h3>No applications yet.</h3><p>Find a shift and apply — it will appear here.</p><a href="/dashboard">Discover shifts →</a></div>}</section></main>}
