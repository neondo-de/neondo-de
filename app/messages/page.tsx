"use client";
import { useState } from "react";
import { ArrowLeft, MessageCircle, Send } from "lucide-react";

export default function MessagesPage(){const [text,setText]=useState("");const [sent,setSent]=useState<string[]>([]);function send(){if(!text.trim())return;setSent(v=>[...v,text.trim()]);setText("")};return <main className="simple-page"><a className="back" href="/dashboard"><ArrowLeft size={16}/> Back to dashboard</a><div className="chat-card"><aside><span className="eyebrow">MESSAGES</span><h1>Messages</h1><div className="conversation active"><div className="chat-avatar">N</div><div><b>NEONDO</b><span>Updates and support</span></div></div><div className="conversation"><div className="chat-avatar muted-avatar">+</div><div><b>Start a conversation</b><span>Open it from a shift or profile</span></div></div></aside><section className="chat-main"><div className="chat-header"><MessageCircle size={18}/><div><b>NEONDO</b><span>Simple, clear communication</span></div></div><div className="chat-body"><div className="message received">Welcome to NEONDO. Messages between crew and companies will appear here.</div>{sent.map((m,i)=><div className="message sent" key={i}>{m}</div>)}</div><div className="composer"><input value={text} onChange={e=>setText(e.target.value)} onKeyDown={e=>e.key==='Enter'&&send()} placeholder="Write a message…"/><button onClick={send}><Send size={16}/></button></div></section></div></main>}
